<%= form_for(@hug) do |f| %>
  <% if @hug.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hug.errors.count, "error") %> prohibited this hug from being saved:</h2>

      <ul>
      <% @hug.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.submit "Get Hugs, ASAP", class:"btn btn-success btn-super" %>
<% end %>
